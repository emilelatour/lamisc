% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fmt_est_ci.R
\name{fmt_est_ci}
\alias{fmt_est_ci}
\title{Format numeric estimates with confidence intervals}
\usage{
fmt_est_ci(
  est,
  lower_ci,
  upper_ci,
  ci_level = 0.95,
  estimate_suffix = "",
  bounds_suffix = "",
  show_ci_label = TRUE,
  dash = c("en", "rmd", "hyphen", "to"),
  pad_dash = FALSE,
  accuracy = NULL,
  scale = 1,
  prefix = "",
  big.mark = "",
  decimal.mark = ".",
  ci_brackets = "()",
  trim = TRUE,
  ...
)
}
\arguments{
\item{est}{Numeric vector of point estimates.}

\item{lower_ci}{Numeric vector of lower confidence interval bounds.}

\item{upper_ci}{Numeric vector of upper confidence interval bounds.}

\item{ci_level}{Confidence level to display in the label. Default is \code{0.95}
(printed as \code{"95\%"}).}

\item{estimate_suffix}{Suffix applied to the formatted point estimate.
Default is \code{""}.}

\item{bounds_suffix}{Suffix applied to the confidence interval bounds.
Default is \code{""}.}

\item{show_ci_label}{Logical; if TRUE (default), includes the confidence level
label (e.g., "95\% CI:") before the interval bounds. If FALSE, only the
bounds are shown, producing output like \code{"12.3 (9.8–14.7)"}.}

\item{dash}{Character string indicating how to display the separator between
bounds. Options are \code{"en"} (en dash, default), \code{"rmd"} (two hyphens, which
knit to an en dash in R Markdown), \code{"hyphen"}, or \code{"to"}.}

\item{pad_dash}{Logical; if \code{TRUE}, adds spaces around the dash (e.g.,
\code{"9.8 – 14.7"} instead of \code{"9.8–14.7"}). Ignored when \code{dash = "to"}.}

\item{accuracy}{Rounding accuracy passed to \code{\link[=fmt_num]{fmt_num()}}.}

\item{scale}{Multiplier for values before formatting. Default is \code{1}.}

\item{prefix}{Character string prefix for formatted numbers (passed to
\code{\link[=fmt_num]{fmt_num()}}).}

\item{big.mark}{Character used as thousands separator (passed to
\code{\link[=fmt_num]{fmt_num()}}).}

\item{decimal.mark}{Character used as decimal point (passed to
\code{\link[=fmt_num]{fmt_num()}}).}

\item{ci_brackets}{Character string giving the characters used to enclose the
confidence interval. Default is \code{"()"}; use \code{"[]"} for square brackets or
any two-character string (e.g., \code{"<>"}).}

\item{trim}{Logical; if \code{TRUE} (default), removes trailing zeros after decimal
marks. Passed to \code{\link[=fmt_num]{fmt_num()}}.}

\item{...}{Additional arguments passed on to \code{\link[=fmt_num]{fmt_num()}}.}
}
\value{
A character vector with formatted estimates and confidence intervals.
}
\description{
A convenience wrapper around \code{\link[=fmt_num]{fmt_num()}} (or similar numeric
formatter) and \code{\link[glue:glue]{glue::glue()}} to format point estimates and confidence
intervals consistently for text and tables.
By default, the output looks like \code{"12.3 (95\% CI: 9.8–14.7)"}.
}
\examples{
library(tibble)
library(dplyr)

# Default: text-style output
tibble(est = 12.3, lower_ci = 9.8, upper_ci = 14.7) |>
  mutate(est_ci = fmt_est_ci(est, lower_ci, upper_ci))
#> "12.3 (95\% CI: 9.8–14.7)"

# Without CI label, with spaces around en dash
tibble(est = 12.3, lower_ci = 9.8, upper_ci = 14.7) |>
  mutate(est_ci = fmt_est_ci(est, lower_ci, upper_ci,
                             show_ci_label = FALSE, pad_dash = TRUE))
#> "12.3 (9.8 – 14.7)"

}
