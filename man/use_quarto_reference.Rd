% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/use_quarto_reference.R
\name{use_quarto_reference}
\alias{use_quarto_reference}
\title{Open the lamisc Quarto reference document}
\usage{
use_quarto_reference(dest = tempdir(), open = TRUE)
}
\arguments{
\item{dest}{Character string. Directory to copy the reference file into.
Defaults to a temporary directory so it never clutters your project.
Set to \code{"."} to copy into the current working directory instead.}

\item{open}{Logical. If \code{TRUE} (default) the file is opened in the RStudio
editor via \code{\link[rstudioapi:navigateToFile]{rstudioapi::navigateToFile()}}.}
}
\value{
The path to the reference \code{.qmd} file, invisibly.
}
\description{
Copies the lamisc Quarto syntax reference into a temporary location and
opens it for viewing or rendering. Use this as a living cheatsheet for
callout boxes, tabsets, columns, figure layouts, and other Quarto HTML
patterns. Copy snippets you need into your analysis, then close it.
}
\examples{
\dontrun{
# Open the reference doc from a temp directory (default)
use_quarto_reference()

# Copy into current working directory instead
use_quarto_reference(dest = ".")
}
}
